# Matrix Faukwaa 恢复指南

> **现象**：插 USB → 灯闪白 → 变全红 → Windows 报「无法识别的 USB 设备」，
> Vial / 本工具连不上（HID 枚举为 0）。
> 主控**仍在运行**（灯在跑），只是 USB 没能枚举成功。

---

## 第 0 步：先别做的事

- **不要反复插拔试灯**（会让 Windows 累积坏的设备实例）
- **不要再发任何灯光命令**
- 不要按 Caps/Shift 等按键试图"唤醒"

---

## 第 1 步（最可能有效）：开机瞬间按住 Esc

QMK 的 **Bootmagic** —— 上电瞬间按住 Esc 会强制清空 EEPROM 配置、
以默认状态启动，正好能治"配置写坏导致起不来"。

1. 拔掉 USB 线
2. **按住 Esc 键不放**
3. 插上 USB
4. 保持按住约 **3 秒**后松开
5. 看 Windows 是否识别

无效就换另一组：**空格 + B**（QMK 默认 Bootmagic 组合之一）。

---

## 第 2 步：键盘自带的固件复位组合

Matrix 官方 FAQ 给的两种：

| 组合 | 作用 | 时长 |
|---|---|---|
| **FN + Q + ESC** | 复位主板（恢复默认设置） | 长按 1–2 秒 |
| **FN + ESC** | 恢复出厂 | 长按 2 秒 |

**关键**：这两个是**固件内部逻辑**，不依赖 USB 通信 ——
即使 Windows 报"无法识别"，只要主控在跑（灯会亮）就能生效。

---

## 第 3 步：彻底断电放电

1. 拔掉 USB 线
2. **等 8–10 分钟**（让主控供电电容完全放电）
3. 如果是三模/带电池版本：翻到底面，把**电源开关拨到 OFF 再拨回 ON**
4. 插到 **主板后置 USB 口**（直插，不要前面板 / 集线器 / 延长线）
5. **换一根确认能传数据**的 USB 线（很多线只做充电）

---

## 第 4 步：清理 Windows 侧的坏设备实例

1. 设备管理器 → 菜单「查看」→ **显示隐藏的设备**
2. 找到「未知设备」/ 带黄色感叹号的项 → **右键卸载设备**
3. 展开「通用串行总线控制器」→ 逐个右键卸载 **USB Root Hub**（会自动重装）
4. **重启电脑**
5. 重启后再插键盘

> 也可以：拔掉**所有** USB 设备 → 关机 → 拔电源等 1 分钟 → 开机。

---

## 第 5 步：换一台电脑验证（重要）

插到**另一台电脑**：
- **能识别** → 问题在你这台 Windows（回到第 4 步）
- **也识别不了** → 问题在键盘固件（走第 6 步）

---

## 第 6 步（终极手段）：进 Bootloader 重刷固件

Vial 系键盘的 Bootloader 标识是 **VIBL**（`vibl:d4f8159c`）。

1. 拔掉 USB
2. 按住 **Esc**（或找 PCB 上的 **RESET 按键/焊盘**）不放
3. 插上 USB，然后松开
4. 设备管理器里应出现 **`Vial Bootloader [xxxx:xxxx]`** 或 VIBL 相关设备
5. 出现后用下面任一工具重刷：
   - **Matrix 官方驱动 / 固件工具**（Matrix Lab 官网下载）
   - **Vial Web**：`https://config.matrix-lab.com/`
   - **QMK Toolbox**（若 VIBL 兼容）

> 进 bootloader 后**键盘不会有正常灯效**，这是正常的。

---

## 为什么会这样（已实测确认的根因）

> ⛔ **根因：往矩阵通道写了越界灯号，穿透了固件的 LED 缓冲区。**

- 矩阵轴灯只有 **0 .. 73**（共 74 颗，来自定义 `amk_rgb_matrix.count`）。
- 指示灯（74）和配件灯条（75–92）**不在矩阵通道里**，要走各自的通道。
- 我当时的错误写法是 `for i in range(0, 93)` —— **越界写了 20 次**，
  固件对 `SET_MATRIX_LED(48, index)` 的 `index` **不做边界检查**，
  于是按 `leds[index]` 写穿了缓冲区 → 内存损坏 → 主循环死机
  → USB 枚举失败、按键无响应。

复现记录（两次事故完全一致）：

| 时间 | 我执行的写操作 | 结果 |
|---|---|---|
| 第 1 次 | `for i in range(0, 93): set_matrix_led(i, off)` | 死机 |
| 第 2 次 | `for i in range(0, 94): set_matrix_led(i, white)` | 死机 |

已排除的原因：

- `_send()` 是**带响应读回**的（不是射后不理），**不是** HID 缓冲区溢出。
- 不是 Windows 端口问题（换端口无效）。
- 不是 custom 档本身的问题；但**卡在 custom 档**是死机后的**伴随状态**
  （还原时实测 `current=45`，切回 `default=13` 后立即恢复）。

### 已落地的代码防护

- `set_matrix_led()` 内置边界检查：**越界直接返回 `False` 且不发包**；
- 新增 `matrix_led_count()`：从键盘定义读 `amk_rgb_matrix.count`；
- `clear_matrix_leds()` 的 `last` 默认 `matrix_led_count()-1`，不再硬编码 92；
- 所有循环改为 `range(0, dev.matrix_led_count())`。

**一句话纪律**：索引上限永远来自 `matrix_led_count()`，**绝不硬编码**。

---

## 待办（工具侧加固，防止再犯）

1. `perkey_set` / `perkey_fill` 写完后**自动把矩阵模式还原到 default(13)**，
   不要把键盘留在 custom 档。
2. 增加写入节流（每颗灯之间 sleep 几毫秒）。
3. 新增 `mvl.py matrix-reset`：一键还原矩阵模式 + 清空所有灯。
4. 把「逐键测试」限制在**已知有效灯号**范围内，不再扫 0–92 的空白地址。
