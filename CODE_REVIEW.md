# vial-matrix-light 代码审查：UI 与后端逻辑优化

> 范围：`vial_light/`（device / gui / theme / transport）+ `mvl.py`。
> 结论分两类：**已实现（本轮已改，可直接验证）** 与 **建议（待拍板）**。

---

## 一、已实现（本轮修复，含真机验证）

### 后端 `device.py`

| # | 问题 | 改动 | 效果 |
|---|---|---|---|
| 1 | `set_matrix_led()` 每写一颗灯就打一次 `matrix_info()` 往返 | **缓存 `matrix_info()`**（`start`/`count` 是设备常量） | `perkey_fill`(68 颗) 从 **136 次往返降到 68 次**，实测 **0.57s**（≈8ms/颗），提速近一倍 |
| 2 | `matrix_row()` 里 `count` 变量算完从未使用，还多打一次往返 | **删除死代码** | 每行少 1 次往返，逻辑更干净 |
| 3 | 往矩阵通道写 `index >= 74` 会穿透固件 LED 缓冲区 | `set_matrix_led()` **内置边界检查**（越界返回 `False` 且不发包） | **杜绝把键盘写死**（本轮已两次复现根因） |

### GUI `gui.py`

| # | 问题 | 改动 | 效果 |
|---|---|---|---|
| 4 | `push_leds` 每次把 **68 颗灯全量重写**，哪怕只改 1 颗 | 新增影子缓冲 `_pushed`，**只发"变了"的灯** | 点 1 颗只发 1 颗（2 次往返），不再 136 次 |
| 5 | 画笔拖动是高频事件，**每个 motion 事件都全量推 68 颗**（≈136 次往返/帧）——UI 拖死，也是把键盘写死的元凶之一 | 拖动改为 `_schedule_pk_push`：**攒 120ms 批量发，且只发拖过的灯** | 拖动不再卡顿，写包数量下降 1~2 个数量级 |
| 6 | 换设备/重载后影子缓冲没清，可能漏推 | `load_leds` 里重置 `_pushed` / `_pk_pending` | 重连后正确按全量推送 |

> 验证：`main.py --selftest` → **SELFTEST OK**；`push_leds` 三个调用点
> （点按 / 图案 / 防抖）与新签名兼容。

---

## 二、建议（待你拍板，按优先级）

### P1 — 中价值，彻底解决「点一下就卡」

**7. HID 调用全部跑在 Tk 主线程 → UI 会冻结**
任何慢操作（读定义、逐键填充、拉取分区）都会让界面无响应。
建议：HID 操作放 **worker 线程 + 结果队列**，主线程只负责画。
改动较大（涉及所有 `dev.*` 调用点），但这是最影响手感的优化。

**8. `_send` 重试策略偏保守**
`retries=4 × sleep(0.1)`，瞬断时单次往返最高 ~0.9s，叠在主线程会明显卡。
建议 GUI 场景降到 1–2 次重试、缩短间隔（`device.py:_send`）。

### P2 — 小清理 / 洁癖

**9. `matrix_row()` 里 `rows` 同样是死变量**（只用了 `cols`）→ 删。

**10. `mvl.py:_key_label_to_rc` 恒返回 `None`** —— 死路径。
`perkey-set` 现在只吃数字灯号，这个键名解析可删或留注释说明。

**11. `theme.py:ScrollArea` 的 `bind_all`** —— 进入接管滚轮、离开解绑。
若两个滚动区叠加、或鼠标从滚动条拖出，理论上可能不解绑。目前未复现，先观察。

**12. `perkey_read` 也是逐颗往返（68 次）** —— 只读命令可接受；
若要快，可在协议允许时加批量读（`GET_RGB_DATA(60)` 的 stride 模式）。

### P3 — 已修复

**13. 「本地动画」对 AMK 后端无效（已修复）**
`_anim_step` 使用的是 `vialrgb_push`（VialRGB 直连通道），因此现在只有已读取
LED 的 VialRGB 设备启用该开关；AMK 和其他后端会置灰，避免点击后才报错。

**14. 全局灯光的实时下发与保存语义不清（已修复）**
灯光页现在维护本地草稿：实时下发关闭时，修改会保留到「应用到设备」；
「保存到固件」会先应用待提交字段，再发送保存命令。连接、读取或断开设备时会
清理待提交状态。

**15. 逐键和分区作用范围混淆（已修复）**
AMK 配件灯在逐键画布中标为只读，颜色、亮度和速度改为当前灯条专属控件；
分区页的配件灯卡片明确是批量应用到全部灯条。逐键单击、拖动和 VialRGB 子集
推送不再默认重写未编辑灯位。

---

## 三、一句话总结

| 维度 | 现状 | 关键改动 |
|---|---|---|
| **正确性** | 越界写会死机 | 已加边界守卫 ✅ |
| **推送效率** | 全量 68 颗/次 | 差量 + 缓存，**降 1~2 个数量级** ✅ |
| **手感** | 拖动 / 慢操作冻结 UI | 拖动已防抖；读写线程化仍是后续可选优化 |
